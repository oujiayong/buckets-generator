package ${common.parentPackageName}.service;

import buckets.framework.base.common.service.BaseService;
import ${common.parentPackageName}.entity.${common.className};

/**
 * ${table.tableComment} 业务接口
 * @author ${common.author}
 * @email ${common.email}
 * @date ${common.date}
 */
public interface ${common.className}Service extends BaseService<${common.className},${table.tableIdType}> {

}