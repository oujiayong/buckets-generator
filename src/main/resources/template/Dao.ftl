package ${common.parentPackageName}.dao;

import ${common.parentPackageName}.entity.${common.className};
import tk.mybatis.mapper.common.Mapper;

/**
 * ${table.tableComment} 数据操作接口
 * @author ${common.author}
 * @email ${common.email}
 * @date ${common.date}
 */
public interface ${common.className}Dao extends Mapper<${common.className}> {
}